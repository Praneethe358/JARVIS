# ╔═══════════════════════════════════════════════════════╗
# ║  NEXUS — requirements.txt                            ║
# ║  Install all:  pip install -r requirements.txt        ║
# ╚═══════════════════════════════════════════════════════╝

# ── Core AI & Voice ──────────────────────────────────────
SpeechRecognition>=3.10    # STT (speech-to-text)
pyttsx3>=2.90              # TTS (text-to-speech, offline)
pyaudio>=0.2.13            # Microphone access

# ── Wake Word ────────────────────────────────────────────
pvporcupine>=3.0.0         # Wake word (optional but recommended)
# pip install pvporcupine   # get access key at picovoice.ai

# ── Computer Vision (Face Auth) ──────────────────────────
opencv-python>=4.9.0
face-recognition>=1.3.0    # requires dlib
# Windows users: pip install dlib-binary (pre-built wheel)
numpy>=1.26.0

# ── Offline STT (optional) ───────────────────────────────
# offline Whisper model      # uncomment for offline transcription
# soundfile

# ── Weather & News ───────────────────────────────────────
requests>=2.31.0

# ── Music (Spotify) ──────────────────────────────────────
spotipy>=2.23.0

# ── System Control ───────────────────────────────────────
pyautogui>=0.9.54
psutil>=5.9.0

# ── Calendar (Google) ────────────────────────────────────
# gcsa>=2.2.0               # uncomment for Google Calendar
# google-auth-oauthlib

# ── Premium TTS (optional) ───────────────────────────────
# elevenlabs>=0.3.0          # natural voice, needs API key

# ── Utilities ────────────────────────────────────────────
python-dotenv>=1.0.0       # load .env for API keys

# ── Stock Market Intelligence ─────────────────────────────────────────────────
yfinance>=0.2.40           # Yahoo Finance stock quotes (no API key needed, fallback)

# ── Jarvis Voice Pipeline ─────────────────────────────────────────────────────
edge-tts>=7.0.0            # Microsoft Neural TTS
sounddevice>=0.4.6         # Fast low-latency audio capture and playback
numpy>=1.26.0              # Audio buffer processing
webrtcvad>=2.0.10          # Voice Activity Detection (silence detection)
requests>=2.31.0           # For NVIDIA Parakeet STT API calls
pydub>=0.25.1              # For decoding MP3 streams to sounddevice
